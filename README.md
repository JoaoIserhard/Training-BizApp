# Training---Biz-App
An elegant Android business card with a hidden portfolio tray. Built using Compose, it demonstrates how to use remember for state management and items for list rendering. The app uses a custom getDrawableResource logic to display specific team logos based on the project data loaded from the app's assets.
